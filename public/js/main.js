// Shared utilities for Sepid Birthday site

document.addEventListener('DOMContentLoaded', () => {
  // Soft entrance already handled by CSS
  // Optional: add subtle parallax or more interactions later
});
